import Image from 'next/image';import Link from 'next/link';import { ContentItem } from '@/lib/markdown';import { formatDate } from '@/lib/utils';
export function PostCard({ item }: { item: ContentItem }) { return (<article className="overflow-hidden rounded-3xl bg-white shadow-soft"><div className="relative h-56"><Image src={item.image || '/uploads/post-default.svg'} alt={item.title} fill className="object-cover" /></div><div className="space-y-4 p-6"><div className="flex flex-wrap items-center gap-3 text-sm text-slate-500">{item.category ? <span className="rounded-full bg-brand-greenSoft px-3 py-1 text-brand-blue">{item.category}</span> : null}{item.date ? <span>{formatDate(String(item.date))}</span> : null}</div><h3 className="text-2xl font-semibold text-slate-900">{item.title}</h3><p className="text-slate-600">{item.description}</p><Link href={`/impacto/${item.slug}`} className="inline-flex font-semibold text-brand-blue hover:text-brand-blueDark">Ler mais</Link></div></article>); }
