'use client';
import Link from 'next/link';import { usePathname } from 'next/navigation';import { Menu } from 'lucide-react';import { useState } from 'react';import { navigation } from '@/lib/site';import { cn } from '@/lib/utils';import { Logo } from './logo';
export function Header() { const pathname = usePathname(); const [open, setOpen] = useState(false); return (<header className="sticky top-0 z-50 border-b border-slate-200/80 bg-white/95 backdrop-blur"><div className="container-shell flex h-20 items-center justify-between gap-6"><Logo /><button type="button" className="inline-flex rounded-full border border-slate-200 p-3 lg:hidden" aria-label="Abrir menu" onClick={() => setOpen((value) => !value)}><Menu className="h-5 w-5" /></button><nav className="hidden items-center gap-1 lg:flex">{navigation.map((item) => (<Link key={item.href} href={item.href} className={cn('rounded-full px-4 py-2 text-sm font-medium transition hover:bg-brand-greenSoft hover:text-brand-blue', pathname === item.href ? 'bg-brand-greenSoft text-brand-blue' : 'text-slate-700')}>{item.label}</Link>))}<Link href="/como-ajudar" className="ml-2 rounded-full bg-brand-blue px-5 py-3 text-sm font-semibold text-white transition hover:bg-brand-blueDark">Doe ou participe</Link></nav></div>{open ? (<div className="border-t border-slate-200 bg-white lg:hidden"><div className="container-shell flex flex-col py-4">{navigation.map((item) => (<Link key={item.href} href={item.href} className={cn('rounded-2xl px-4 py-3 text-sm font-medium', pathname === item.href ? 'bg-brand-greenSoft text-brand-blue' : 'text-slate-700')} onClick={() => setOpen(false)}>{item.label}</Link>))}</div></div>) : null}</header>); }
