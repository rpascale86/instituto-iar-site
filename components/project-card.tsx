import Image from 'next/image';import Link from 'next/link';import { ContentItem } from '@/lib/markdown';import { formatDate } from '@/lib/utils';
export function ProjectCard({ item }: { item: ContentItem }) { return (<article className="overflow-hidden rounded-3xl bg-white shadow-soft"><div className="relative h-56"><Image src={item.image || '/uploads/projeto-default.svg'} alt={item.title} fill className="object-cover" /></div><div className="space-y-4 p-6">{item.date ? <p className="text-sm text-slate-500">{formatDate(String(item.date))}</p> : null}<h3 className="text-2xl font-semibold text-slate-900">{item.title}</h3><p className="text-slate-600">{item.description}</p><Link href={`/projetos/${item.slug}`} className="inline-flex font-semibold text-brand-blue hover:text-brand-blueDark">Ver projeto</Link></div></article>); }
