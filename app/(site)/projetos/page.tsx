import { PageHero } from '@/components/page-hero';import { ProjectCard } from '@/components/project-card';import { getCollection } from '@/lib/markdown';
export default function ProjetosPage() { const projects = getCollection('projects'); return (<><PageHero title="Projetos" description="Conheça os programas e iniciativas do Instituto IAR voltados para segurança alimentar, fortalecimento comunitário e educação." /><section className="section-space"><div className="container-shell grid gap-6 lg:grid-cols-3">{projects.map((item) => (<ProjectCard key={item.slug} item={item} />))}</div></section></>); }
