import { PageHero } from '@/components/page-hero';import { PostCard } from '@/components/post-card';import { getCollection } from '@/lib/markdown';
export default function ImpactoPage() { const posts = getCollection('posts'); return (<><PageHero title="Impacto e Ações" description="Blog e central de notícias para divulgar ações comunitárias, eventos, campanhas, resultados e histórias de transformação." /><section className="section-space"><div className="container-shell grid gap-6 lg:grid-cols-3">{posts.map((item) => (<PostCard key={item.slug} item={item} />))}</div></section></>); }
