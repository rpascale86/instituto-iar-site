import Image from 'next/image';import { notFound } from 'next/navigation';import type { Metadata } from 'next';import { getCollection, getItemBySlug, markdownToHtml } from '@/lib/markdown';import { absoluteUrl, formatDate } from '@/lib/utils';
export async function generateStaticParams() { return getCollection('posts').map((item) => ({ slug: item.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const item = getItemBySlug('posts', slug); if (!item) return {}; return { title: item.title, description: item.description, openGraph: { title: item.title, description: item.description, images: item.image ? [absoluteUrl(item.image)] : [] } }; }
export default async function PostDetalhePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = getItemBySlug('posts', slug); if (!item) notFound(); const html = await markdownToHtml(item.content); return (<section className="section-space"><div className="container-shell max-w-4xl"><article className="overflow-hidden rounded-[2rem] bg-white shadow-soft"><div className="relative h-72 sm:h-96"><Image src={item.image || '/uploads/post-default.svg'} alt={item.title} fill className="object-cover" /></div><div className="space-y-6 p-8 sm:p-10"><div className="flex flex-wrap gap-3 text-sm text-slate-500">{item.category ? <span className="rounded-full bg-brand-greenSoft px-3 py-1 text-brand-blue">{item.category}</span> : null}{item.date ? <span>{formatDate(String(item.date))}</span> : null}</div><h1 className="text-4xl font-bold tracking-tight text-slate-900">{item.title}</h1><p className="text-lg leading-8 text-slate-600">{item.description}</p><div className="prose-content" dangerouslySetInnerHTML={{ __html: html }} /></div></article></div></section>); }
